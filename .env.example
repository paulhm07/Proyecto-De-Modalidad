# URL del backend de NestJS en producción (ej. https://mundilex-backend.onrender.com)
# Si se deja vacío en desarrollo local, se usa el proxy interno /api -> http://localhost:3001
NEXT_PUBLIC_API_URL=
